# ========================================
# OrbitAI Environment Variables
# ========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ----------------------------------------
# Database
# ----------------------------------------
# PostgreSQL connection string from Supabase or your own PostgreSQL instance
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://your-user:your-password@your-host:5432/your-database

# ----------------------------------------
# Supabase (Required for Authentication)
# ----------------------------------------
# Get these from your Supabase project settings: https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase service role key (SECRET - server-side only, has admin privileges)
# ⚠️ NEVER expose this key to the frontend!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----------------------------------------
# Frontend Environment Variables
# (These are exposed to the client via Vite's import.meta.env)
# ----------------------------------------

# Supabase project URL (same as above, but for frontend)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anon/public key (safe to expose to frontend)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# API base URL (optional - defaults to localhost in dev)
# VITE_API_URL=http://localhost:5001

# ----------------------------------------
# OpenAI (Required for AI features)
# ----------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ----------------------------------------
# Application Configuration
# ----------------------------------------
# Environment: development | production | test
NODE_ENV=development

# Server port
PORT=5001

# ----------------------------------------
# Security Notes
# ----------------------------------------
# 1. NEVER commit .env files to version control
# 2. NEVER expose SUPABASE_SERVICE_ROLE_KEY to the frontend
# 3. NEVER expose OPENAI_API_KEY to the frontend
# 4. Rotate keys immediately if accidentally exposed
# 5. Use different keys for development and production

# ----------------------------------------
# Setup Instructions
# ----------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values
# 3. Run: npm install
# 4. Run database migrations: npm run prisma:migrate:dev
# 5. Start development server: npm run dev
